{
  "name": "photo-album-organizer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "A photo management application that helps users organize photos into date-grouped albums with drag-and-drop reordering capabilities",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "lint": "eslint src --ext .js",
    "lint:fix": "eslint src --ext .js --fix"
  },
  "dependencies": {
    "sql.js": "^1.8.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "eslint": "^9.17.0",
    "happy-dom": "^20.0.8",
    "jsdom": "^27.0.1",
    "vite": "^5.4.10",
    "vitest": "^2.1.8"
  }
}
